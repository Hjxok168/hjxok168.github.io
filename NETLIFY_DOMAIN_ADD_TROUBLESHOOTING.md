# Netlify域名添加问题排查指南

本指南专门帮助您解决在Netlify中无法添加hxqok.wuaze.com域名的问题。

## 一、检查域名状态

1. 首先确认hxqok.wuaze.com域名是否已经注册成功
   - 可以使用在线WHOIS查询工具（如 https://whois.chinaz.com/）查询域名状态
   - 检查域名是否处于活动状态，是否有正确的DNS解析记录

2. 确认您是否拥有该域名的管理权限
   - 需要能访问域名注册商的管理后台
   - 能够修改域名的DNS记录

## 二、常见错误及解决方案

### 错误1：域名所有权验证失败

**现象**：Netlify提示无法验证域名所有权

**解决方案**：

1. 等待域名完全注册生效（新注册域名可能需要24-48小时全球DNS生效）
2. 尝试以下验证方法：
   - 方法一：在Netlify中使用TXT记录验证
     - 登录Netlify，进入域名设置
     - 获取Netlify提供的TXT记录值
     - 在域名注册商的DNS管理中添加该TXT记录
     - 等待DNS记录生效（通常需要10-30分钟）
     - 点击Netlify中的验证按钮
   - 方法二：使用CNAME记录临时验证
     - 在域名注册商处添加一条CNAME记录，将hxqok.wuaze.com指向您的Netlify临时域名(graceful-narwhal-bea10a.netlify.app)
     - 等待DNS生效后再尝试添加域名

### 错误2：域名已被其他Netlify账户使用

**现象**：Netlify提示域名已被注册

**解决方案**：

1. 确认该域名是否确实属于您，或者是否曾被其他人使用
2. 如果确定是您的域名但被其他账户占用：
   - 联系Netlify客服（support@netlify.com）提供域名所有权证明，请求域名释放
   - 提供域名注册信息的截图作为证明

### 错误3：DNS记录配置不正确

**现象**：Netlify无法检测到正确的DNS配置

**解决方案**：

针对hxqok.wuaze.com域名，请按照以下步骤配置DNS记录：

1. **CNAME记录配置方法**（推荐）：
   - 登录您的域名注册商后台
   - 找到DNS管理设置
   - 添加一条CNAME记录：
     - 主机名：hxqok
     - 记录类型：CNAME
     - 记录值：graceful-narwhal-bea10a.netlify.app
     - TTL：3600（或默认值）

2. **A记录配置方法**（备用）：
   - 如果您的域名注册商不支持CNAME根记录，可使用A记录
   - 添加以下A记录：
     - 主机名：hxqok
     - 记录类型：A
     - 记录值：75.2.60.5
     - TTL：3600
   - 再添加另一条A记录（可选，提高可用性）：
     - 主机名：hxqok
     - 记录类型：A
     - 记录值：99.83.190.102
     - TTL：3600

3. **等待DNS记录生效**
   - DNS更改通常需要10分钟到48小时才能全球生效
   - 可以使用 https://www.whatsmydns.net/ 工具检查DNS记录是否已正确传播

## 三、分步操作指南

### 步骤1：准备工作

1. 确保您已登录Netlify账户，并找到了您的网站
2. 确保您能访问hxqok.wuaze.com域名的DNS管理界面
3. 准备好记录DNS更改的时间点

### 步骤2：在Netlify中添加域名

1. 登录Netlify，进入您的网站仪表盘
2. 点击左上角的"Site settings"（网站设置）
3. 在左侧菜单中选择"Domain management"（域名管理）
4. 点击"Add custom domain"（添加自定义域名）
5. 输入hxqok.wuaze.com，然后点击"Save"（保存）
6. 此时系统会显示一个验证界面，请不要关闭

### 步骤3：配置DNS记录

1. 打开新的浏览器标签页，登录您的域名注册商后台
2. 找到DNS设置页面
3. 根据Netlify显示的验证要求，添加相应的DNS记录（TXT或CNAME）
4. 保存DNS设置更改

### 步骤4：验证域名

1. 返回Netlify的域名添加页面
2. 等待5-10分钟让DNS记录生效
3. 点击"Check DNS configuration"（检查DNS配置）按钮
4. 如果验证成功，您会看到成功提示
5. 接着按照系统提示启用SSL/TLS加密

## 四、如果仍然失败怎么办？

如果按照上述步骤操作后仍然无法添加域名，请尝试以下方法：

1. **使用备用域名添加方法**：
   - 先尝试添加www.hxqok.wuaze.com子域名
   - 成功后再尝试添加hxqok.wuaze.com

2. **联系Netlify客服**：
   - 访问Netlify支持页面：https://www.netlify.com/support/
   - 提供详细的错误信息和您已尝试的操作
   - 等待客服回复（通常24小时内）

3. **临时解决方案**：
   - 继续使用Netlify提供的临时域名（graceful-narwhal-bea10a.netlify.app）
   - 等待24-48小时后再尝试添加域名，可能是DNS传播延迟问题

## 五、常见问题解答

**Q: 添加域名时出现"Invalid domain"错误怎么办？**
A: 这可能是因为域名格式不正确，或者Netlify不支持该域名后缀。请确认域名拼写正确，并检查wuaze.com是否是Netlify支持的域名后缀。

**Q: DNS记录配置正确但Netlify仍然检测不到怎么办？**
A: 清除浏览器缓存，或尝试使用不同的浏览器。也可以使用命令行工具如`nslookup`或`dig`检查DNS记录是否真的生效。

**Q: 添加域名后需要多长时间才能通过新域名访问网站？**
A: 通常在域名添加成功并配置正确后，几分钟到几小时内就可以通过新域名访问。但完全的全球DNS传播可能需要24-48小时。

如果您在操作过程中遇到其他问题，请随时记录详细的错误信息，并联系域名注册商或Netlify客服获取进一步的帮助。