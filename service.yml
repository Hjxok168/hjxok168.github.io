apiVersion: v1
kind: Service
metadata:
  name: mywebapp-service
  labels:
    app: mywebapp
spec:
  type: LoadBalancer  # 使用LoadBalancer类型，适用于TKE环境
  selector:
    app: mywebapp
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http